<UserControl x:Class="PL.Helpers.Items.Icon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="40" Width="40"
             d:DesignHeight="100" d:DesignWidth="100">

    <UserControl.Resources>
        <Storyboard x:Key="NeonPulseAnimation" RepeatBehavior="Forever" AutoReverse="True">

            <DoubleAnimation Storyboard.TargetName="NeonGlow"
                             Storyboard.TargetProperty="BlurRadius"
                             From="10" To="40" Duration="0:0:1.5" />

            <DoubleAnimation Storyboard.TargetName="NeonGlow"
                             Storyboard.TargetProperty="Opacity"
                             From="0.6" To="1.0" Duration="0:0:1.5" />

            <ColorAnimation Storyboard.TargetName="NeonStroke"
                            Storyboard.TargetProperty="Color"
                            From="#00F0FF" To="#0080FF" Duration="0:0:1.5" />
        </Storyboard>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard Storyboard="{StaticResource NeonPulseAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Viewbox Stretch="Uniform">
        <Path Data="M 10,0 L 50,0 L 60,10 L 60,40 L 50,50 L 10,50 L 0,40 L 0,10 Z 
                    M 5,12 L 5,38 M 55,12 L 55,38 
                    M 15,25 L 45,25 M 30,10 L 30,40"
              StrokeThickness="2"
              Fill="#CC111111">
            <Path.Stroke>
                <SolidColorBrush x:Name="NeonStroke" Color="#00F0FF"/>
            </Path.Stroke>

            <Path.Effect>
                <DropShadowEffect x:Name="NeonGlow" 
                                  Color="#00ADEF" 
                                  Direction="0" 
                                  ShadowDepth="0" 
                                  BlurRadius="20" 
                                  Opacity="1"/>
            </Path.Effect>
        </Path>
    </Viewbox>
</UserControl>